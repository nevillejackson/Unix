/* decl and prot for folli.c */

void density(double surfarea,
		double *dens,
		double *pdens,
		double *sodens,
		double *sddens,
		double *sdens,
		double *ratio,
                int time,
                double* follno,
		double* pfollno,
		double* sofollno,
		double* sdfollno);
